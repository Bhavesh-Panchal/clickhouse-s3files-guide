apiVersion: v1
kind: Service
metadata:
  name: clickhouse
  namespace: production
spec:
  type: ClusterIP
  ports:
  - port: 9000
    targetPort: 9000
    name: native
  - port: 8123
    targetPort: 8123
    name: http
  selector:
    app: clickhouse
